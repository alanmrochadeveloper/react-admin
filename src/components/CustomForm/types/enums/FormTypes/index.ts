export enum FormTypes {
  TEXT = 'text',
  EMAIL = 'email',
  NUMBER = 'number',
  PASSWORD = 'password',
  CHECKBOX = 'checkbox',
  SELECT = 'select',
  HIDDEN = 'hidden',
  FILE = 'file'
}

export enum RequestType {
  POST = 'post',
  PATCH = 'update',
  PUT = 'patch'
}
