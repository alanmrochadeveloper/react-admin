export enum FormTypes {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
  CHECKBOX = 'checkbox',
  SELECT = 'select',
  HIDDEN = 'hidden'
}

export enum RequestType {
  POST = 'post',
  PATCH = 'update',
  PUT = 'patch'
}
